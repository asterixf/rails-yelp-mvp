<div class="container col-8">
<h1>New Review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
<%= f.input :rating, :collection => 0..5  %>
<%= f.input :content, class: "col-8 row-5" %>
<%= f.button :submit, "Save", class: "btn btn-primary" %>
<% end %>

</div>
